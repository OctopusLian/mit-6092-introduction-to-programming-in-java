
```
将DrawGraphics改为包含一个Bouncer对象的ArrayList。这与之前作业中的操作相同。

运行你的程序。它应该在屏幕上绘制与之前相同的内容，只是现在应该使用ArrayList。

向ArrayList添加第二个矩形并使其移动。运行你的程序，你现在应该有两个矩形。

创建一个新类，并为其命名一个描述你想制作的形状的名称（例如，Line，Oval，Star，Text，Arrow）。

使这个类实现Sprite接口。你需要添加接口中所有方法的实现。现在，让这些方法什么也不做（例如，空方法，返回0）。

用你自己的对象替换DrawGraphics中的一个矩形。这意味着ArrayList将包含一个矩形和你自己的对象。运行程序。你现在应该只看到一个矩形，因为你的对象还没有代码在屏幕上绘制。

在你的对象的draw()方法中添加代码以绘制你想要的形状。运行程序，直到它看起来你想要的样子。提示：你可能想使用Rectangle作为例子。

此时，你的形状可能不会正确反弹。它可能会在窗口的右边和底边掉落，而不会改变方向。原因是你需要从getWidth()和getHeight()返回正确的宽度和高度，以便Bouncer知道何时改变方向。修复这两个方法以返回适当的值。

最后，当你运行程序时，你应该有一个矩形和一个你自己的形状在屏幕上移动，并在边缘反弹。如果你愿意，可以添加更多的形状。
```
